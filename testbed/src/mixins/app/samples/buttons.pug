mixin button--button(variant)
  p: button(class!=attributes.class) <!-- no value -->
  p: button(class!=attributes.class) #{ variant } button
  p: button(class!=attributes.class type='submit') #{ variant } button (submit)
  p: button(class!=attributes.class) Da da da da da da da da BATMAN!
  p: button(class!=attributes.class) Button with a #[br] line break

mixin button
  button(class!=attributes.class type='button')
    block

mixin button--input(variant)
  input(class!=attributes.class type='button' value=variant + ' input button')

mixin button--a(variant)
  a(class!=attributes.class href='#' role='button') #{ variant } button

mixin button--active()
  each variant in ['primary', 'secondary', 'success', 'warning', 'danger', 'info']
    p: button(class='bsk-btn bsk-btn-' + variant + ' bsk-active') #{ variant } (active)

mixin button--disabled()
  each variant in ['default', 'primary', 'secondary', 'success', 'warning', 'danger', 'info']
    p: button(class='bsk-btn bsk-btn-' + variant disabled) #{ variant } (disabled)

mixin button--size(size)
  p: button(class!=attributes.class) #{ size } button

mixin button--ms-account-size(size)
  p: button(class!=attributes.class)
    object.bsk-ms-pictogram(type='image/svg+xml' data='/' + testbed_version + '/dist/img/logos-symbols/ms-pictogram.svg')
    |
    | #{ size }

mixin button-group--size(size)
  div.bsk-btn-group.app-spacing-bottom(class!=attributes.class role='group')
    +button(class='bsk-btn bsk-btn-primary')
      | #{ size } one
    +button(class='bsk-btn bsk-btn-primary')
      | #{ size } two
    +button(class='bsk-btn bsk-btn-primary')
      | #{ size } three
